import HeapList

var arr = HeapList();

arr.init(["zero", "one", "two", "three", "four", "five", "six"]);
arr.put(7, "seven");
print(arr.toList());
print(arr.size());
print(arr.get(5));

print("========")

/* test iterator */
it := arr.iterator()

while it.hasNext() {
	print(it.next())
}
